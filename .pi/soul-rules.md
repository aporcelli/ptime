# Soul Rules — Loaded by ALL agents (Pi, Agy, Orca, etc.)

Every agent MUST read this file at session start.
These rules are non-negotiable and apply across all CLI/agent runtimes.

---

## Rule 1: Engram Agent Tagging
EVERY agent (Pi, Agy, Orca, or any CLI) MUST include their agent identifier as the first line of every Engram observation:

```
agent: <name>
```

Valid values: `agent: pi`, `agent: agy`, `agent: orca`, etc.

This ensures any future session or agent can trace who created each memory.

---

## Rule 2: Plan Mode Gate
Before ANY edit, write, bash, or git commit, FIRST check if PLAN MODE is active.
If active → ONLY analyze, plan, and propose. Explicitly tell user: "Estoy en plan mode."

---

## Rule 3: CHANGELOG Before Push
Every functional code change MUST include updating CHANGELOG.md with the version entry BEFORE the commit. Never push without changelog already updated.

---

## Rule 4: Version Bump on Every Code Change
Every functional change (fix, feature) MUST:
1. Bump version in package.json
2. npm install (syncs package-lock.json)
3. Update Sidebar.tsx version string
4. Update CHANGELOG.md
5. Commit message includes version

DO NOT bump for docs, reorganization, cleanup, or formatting only.

---
*Generated by Pi el Gentleman — 2026-08-05*
